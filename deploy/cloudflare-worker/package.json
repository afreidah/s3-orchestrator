{
  "name": "s3-orchestrator-cloudflare-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Bandwidth Alliance edge proxy for s3-orchestrator backends",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
