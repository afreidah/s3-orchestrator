# -------------------------------------------------------------------------------
# S3 Orchestrator - Local Dev Secret (k3d + docker-compose)
#
# Author: Alex Freidah
#
# Pre-populated credentials matching the docker-compose.test.yml services.
# Not for production use.
# -------------------------------------------------------------------------------

apiVersion: v1
kind: Secret
metadata:
  name: s3-orchestrator
  namespace: s3-orchestrator
  labels:
    app.kubernetes.io/name: s3-orchestrator
type: Opaque
stringData:
  DB_PASSWORD: "s3proxy"
  BUCKET_ACCESS_KEY: "photoskey"
  BUCKET_SECRET_KEY: "photossecret"
  MINIO1_ACCESS_KEY: "minioadmin"
  MINIO1_SECRET_KEY: "minioadmin"
  MINIO2_ACCESS_KEY: "minioadmin"
  MINIO2_SECRET_KEY: "minioadmin"
  UI_ADMIN_KEY: "admin"
  UI_ADMIN_SECRET: "admin"
